# pi-remote-bridge

```bash
npm run dev            # tsx watch, 端口 30150
npm run typecheck
npm test               # 单元测试 (node --test)
npm run test:e2e       # pi 后端端到端，需要先跑起服务
```

主要尝试重启server, 代码变动后watch会自动重启
