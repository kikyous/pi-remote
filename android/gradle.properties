org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=false

# AGP 8.x needs a JDK 17 daemon. The system default here is JDK 8, so point at
# the Temurin 17 in ~/tools. Change or delete this line on another machine
# whose JAVA_HOME is already 17+.
org.gradle.java.home=/Users/chen/tools/jdk-17.0.20+8/Contents/Home

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official
