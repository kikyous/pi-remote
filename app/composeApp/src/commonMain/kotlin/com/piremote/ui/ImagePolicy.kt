package com.piremote.ui

/** Cap for one prompt's image attachments. */
const val MAX_ATTACHMENTS = 5

/** Longest edge for picked images, scaled down before re-encode. */
const val MAX_IMAGE_EDGE = 2048

/** JPEG/PNG quality when re-encoding a picked image for the wire. */
const val IMAGE_QUALITY = 85
